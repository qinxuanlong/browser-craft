# PageBox 功能需求说明书 (PRD / Functional Requirements)

> **版本**：v1.0.0  
> **文档状态**：初稿（开放补充中）  
> **适用终端**：Chrome / Edge 等 Chromium 内核浏览器（Manifest V3）

---

## 1. 项目背景与产品定位

### 1.1 背景
日常使用浏览器时，用户常因查阅资料、多任务并行而打开数十甚至上百个标签页，造成：
- **内存与性能消耗**：大量后台闲置页面占用系统资源。
- **信息过载与混乱**：难以快速定位目标标签页，窗口关闭后丢失上下文。
- **书签栏痛点**：原生书签栏适合长期静态收藏，但不适合处理“临时堆积、批次处理、稍后阅读”的高频工作流。

### 1.2 产品定位
**PageBox** 是一款轻量、快捷、低侵入的浏览器标签页与窗口收藏管理扩展，专为多标签重度用户打造。提供「一键暂存释放内存」、「结构化搜索恢复」、「浏览器书签同步」以及「双形态交互（Popup / Side Panel）」体验。

---

## 2. 总体功能矩阵与现状

| 模块 | 功能项 | 当前状态 | 优先级 | 简要说明 |
| :--- | :--- | :---: | :---: | :--- |
| **收藏采集** | 收藏当前活动标签页 | ✅ 已实现 | P0 | 过滤非普通网页，记录标题/URL/Favicon |
| | 收藏当前窗口全量标签 | ✅ 已实现 | P0 | 批量打包当前窗口所有网页并自动命名 |
| | 批量选择标签收藏 | ⏳ 待补充 | P1 | 勾选部分标签页暂存 |
| | 快捷键 / 右键菜单收藏 | ⏳ 待补充 | P1 | 降低采集路径操作成本 |
| **展示与恢复** | 标签页恢复打开 | ✅ 已实现 | P0 | 点击新开标签页 |
| | 窗口批量恢复 | ✅ 已实现 | P0 | 一键还原整组窗口标签页 |
| | 恢复后自动清除选项 | ⏳ 待补充 | P2 | 类似“阅后即焚”的稍后读流程 |
| **检索与筛选** | 全文搜索（标题/URL/备注） | ✅ 已实现 | P0 | 实时模糊匹配过滤 |
| | 标签（Tags）筛选 | ⏳ 待补充 | P1 | 按自定义标签过滤列表 |
| | 文件夹层级筛选 | ⏳ 待补充 | P1 | 切换目录树浏览 |
| **组织管理** | 备注编辑 | ✅ 已实现 | P0 | 弹窗修改单个标签页备注 |
| | 文件夹层级与树状视图 | ✅ UI已支持展示 | P0 | 支持树状折叠、子目录嵌套与未分类项展示 |
| | 文件夹创建/编辑/删除 | ⏳ 待UI补充 | P1 | 底层已支持创建，需提供前端创建与重命名入口 |
| | 标签（Tags）管理 | ⏳ 待补充 | P1 | 支持自定义打标、颜色标记 |
| | 拖拽排序（Drag & Drop） | ✅ 已实现 | P0 | 列表内自由拖动调整顺序与拖动标签至左侧文件夹归类 |
| **数据备份** | JSON 导出与导入 | ✅ 已实现 | P0 | 本地备份与跨设备导入（支持合并） |
| | 浏览器书签双向同步 | ✅ 已实现 | P0 | 支持「浏览器➔插件」拉取与「插件➔浏览器」安全推送双向同步 |
| | 云端多端同步 | ⏳ 待规划 | P3 | WebDAV / GitHub Gist / 独立服务账号体系 |
| **交互形态** | Popup 弹出层入口 | ✅ 已实现 | P0 | 工具栏点击快速轻量操作 |
| | Side Panel 侧边栏常驻 | ✅ 已实现 | P0 | 适合宽屏分屏工作流，持续常驻不关闭 |

---

## 3. 功能详细需求说明

### 3.1 标签页与窗口收藏 (Collection)

#### 3.1.1 收藏当前标签页
- **触发入口**：顶部工具栏「收藏标签」按钮、快捷键（待定）。
- **行为逻辑**：
  1. 获取当前窗口中活跃（Active）的标签页信息（Title, URL, FavIconUrl）。
  2. 校验协议合法性（目前仅允许 `http:`、`https:`、`file:` 协议，过滤 `chrome://` 等浏览器保留页）。
  3. 存入本地存储，生成唯一 UUID，写入创建与更新时间戳。
  4. 触发界面即时刷新与成功反馈。
- <!-- 用户补充：收藏标签页逻辑补充 -->

#### 3.1.2 收藏当前窗口
- **触发入口**：顶部工具栏「收藏窗口」按钮。
- **行为逻辑**：
  1. 抓取当前窗口中全部打开的 Tab。
  2. 过滤无 URL 的异常标签，其余标签保留标题、URL、图标。
  3. 默认生成窗口名称（如：`窗口 YYYY/MM/DD HH:mm:ss`），允许后续重命名或补充备注。
  4. 支持一键批量恢复。
- <!-- 用户补充：收藏窗口逻辑补充 -->

---

### 3.2 组织与分类 (Organization)

#### 3.2.1 文件夹系统（Folder Tree）
- **需求目标**：解决收藏条目增多后的归类管理需求。
- **现状分析**：
  - 数据层已实现 `Folder` 模型与 `createFolder` 接口。
  - 前端已实现 `FolderTree` 折叠树组件，支持多级目录树展开/折叠、未分类统计、标签恢复与备注操作。
- **待完善交互**：
  - 支持在 UI 上直接「新建文件夹」、「重命名文件夹」、「删除文件夹」。
  - 收藏新标签页时，允许下拉选择归属于哪个文件夹。
  - 支持将标签页移动或拖拽至指定文件夹。
- <!-- 用户补充：文件夹功能细化要求 -->

#### 3.2.2 标签（Tags）与备注系统
- **需求目标**：支持横向维度的内容归纳和个人记忆补充。
- **现有逻辑**：每个标签与窗口均包含 `notes?: string` 与 `tags: string[]` 字段。
- **待完善点**：
  - 目前仅实现了单个 Tab 的备注编辑弹窗。
  - 需要补充：标签的新增、删除、点击标签即过滤列表。
- <!-- 用户补充：标签与备注功能要求 -->

#### 3.2.3 拖拽排序与移动（Drag & Drop）
- 列表项支持按用户习惯上下拖拽调整显示顺序。
- 支持直接拖入指定文件夹。
- <!-- 用户补充：拖拽排序交互要求 -->

---

### 3.3 搜索与检索 (Search & Filter)

- **实时检索**：输入框变动即时对内存数据进行多维度过滤（Title、URL、Notes、Tags）。
- **复合过滤（待补充）**：
  - 按类型筛选（仅看标签 / 仅看窗口 / 仅看书签导入项）。
  - 按时间范围过滤（今天、近7天、近1月）。
- <!-- 用户补充：搜索与筛选要求 -->

---

### 3.4 恢复与打开策略 (Restore)

- **单标签恢复**：
  - 默认行为：在新标签页中打开指定 URL，当前视窗跳转至新标签。
  - 配置项（待补充）：是否前台激活、是否在当前标签页覆盖打开。
- **窗口恢复**：
  - 默认行为：依次在新标签页中打开所有窗口内页面（后台加载）。
  - 扩展策略（待补充）：是否恢复为一个崭新的独立浏览器窗口？还是合并到当前窗口？
- **消耗策略（待补充）**：
  - 恢复后是否保留在收藏列表？还是可配置“打开即从待办归档/删除”？
- <!-- 用户补充：恢复策略要求 -->

---

### 3.5 数据备份与同步 (Sync & Backup)

#### 3.5.1 本地 JSON 导入与导出
- **导出**：将全量 store 数据封装为带时间戳与版本号的标准 JSON 文件，触发浏览器下载。
- **导入**：
  - 支持导入校验（验证结构合法性）。
  - 模式选择：**合并模式（Merge）**（保留现有数据并追加）与 **覆盖模式（Overwrite）**。
- <!-- 用户补充：导入导出格式或逻辑要求 -->

#### 3.5.2 浏览器书签双向同步
- **方向一：浏览器 ➔ 插件（拉取）**：
  - 调用 `chrome.bookmarks.getTree()` 扫描浏览器书签树。
  - 自动在 PageBox 中创建对应名称的文件夹，生成带 `"浏览器书签"` 标签的 Tab 项。
  - 重新同步时，幂等清空旧的书签同步项，避免重复堆叠。
- **方向二：插件 ➔ 浏览器（推送）**：
  - 在浏览器书签中创建独立的「**PageBox 收藏**」专用根目录，隔离保护原有书签。
  - 递归重建 PageBox 中的所有文件夹层级，将保存的网页标签完整写回到浏览器中。
  - 支持勾选“仅同步 PageBox 原生收藏（排除从浏览器拉取的项）”，防止循环冗余。
- **交互形式**：提供独立的「书签双向同步中心」弹窗，清晰区分两个方向与状态反馈。

#### 3.5.3 云端同步（待规划）
- 潜在路线：
  - 方案 A：WebDAV 同步（坚果云、Nextcloud 等）。
  - 方案 B：GitHub / Gitee Gist 秘钥同步。
  - 方案 C：自定义云端后端 API。
- <!-- 用户补充：云端同步预期方案 -->

---

### 3.6 交互形态与界面 (UI / UX)

#### 3.6.1 Popup 弹窗视图
- 规格受限（一般宽 360px~420px，高 500px~600px）。
- 侧重快速捕捉、快速检索和即时打开。
- 提供「打开侧边栏」快捷跳转。

#### 3.6.2 Side Panel 侧边栏视图
- 高度随浏览器窗口自适应，适合持续展开。
- 支持宽屏左右布局（如左侧文件夹树，右侧条目列表）。
- 需要更舒适的阅读与管理空间。

#### 3.6.3 主题与国际化（待补充）
- 深色模式（Dark Mode）/ 跟随系统主题。
- 多语言支持（i18n）：中文 / 英文。
- <!-- 用户补充：UI 风格、配色或视觉交互要求 -->

---

## 4. 非功能性需求

1. **性能与资源占用**：
   - 必须保持极简后台唤起开销，Background Service Worker 在闲置时自动休眠。
   - 大量收藏项（千级以上）时，列表渲染需保持流畅（可评估引入虚拟滚动）。
2. **权限最小化**：
   - 遵循 Manifest V3 最小权限原则，目前包含 `tabs`, `storage`, `sidePanel`, `bookmarks`。
3. **稳定性与容错**：
   - 存储层严格处理异常，避免非法 URL 导致插件白屏崩溃。
   - 数据存储前保持类型契约校验。

---

## 5. 用户需求补充专区 (TODO / 留白专区)

> **提示**：您可以在下方直接罗列、勾选或细化您的个人业务需求：

- [ ] **补充项 1**：
- [ ] **补充项 2**：
- [ ] **补充项 3**：
- [ ] **补充项 4**：
- [ ] **补充项 5**：

---
