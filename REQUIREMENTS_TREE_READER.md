# TreeReader 功能需求说明书 (PRD / Functional Requirements)

> **项目代号**：TreeReader（树读 - 结构化文本、代码与目录树独立标签页查看器）  
> **版本**：v1.3.0  
> **文档状态**：新标签页独立大屏纯净版  
> **适用终端**：Chrome / Edge 等 Chromium 内核浏览器（Manifest V3 · New Tab 全屏查看）

---

## 1. 项目背景与产品定位

### 1.1 背景与用户痛点
- **多类型文件查看上下文割裂**：用户在浏览器中不仅常需查阅小说章节、设定大纲、Markdown 文档，还经常需要随时参考本地或导出的**代码片段（JS/TS/Python/JSON 等）**、**配置文件（YAML/ENV/INI）**或**系统日志（LOG/TXT）**。
- **避免臃肿 IDE**：随手查阅轻量代码、文档或日志时，启动 VSCode 或重量级编辑器开销大。用户需要一个常驻浏览器侧边栏（Side Panel）的轻量文件查看器。
- **纯粹、无干扰体验**：界面不包含任何收费或商业化推广卡片，保持极致的纯净、专注与轻量。

### 1.2 产品定位
**TreeReader** 是一款专为浏览器原生侧边栏打造的**纯净、完全免费的结构化文本、代码与文件树阅读插件**。  
核心设计理念：**多级树形导航 + 多引擎阅读（Markdown/代码高亮/纯文本）+ 极致纯净免打扰 + 纯本地离线隐私安全**。

---

## 2. 界面设计对照（纯净版布局解析）

界面采用经典 **双栏响应式布局（左侧导航 + 右侧阅读/代码区）**，去除任何付费推广内容：

```
+---------------------------------------------------------------------------------------------------------+
| [📁][📄][🔍][⚙] |  [◨ 收起侧栏]   [🔤 视图模式: Markdown ▾]         [📋 复制] [🔢 行号]     [☰ 章节大纲]  |
|-----------------|---------------------------------------------------------------------------------------|
| > 📁 封面       | 结婚第四年，创业成功的丈夫深夜跪在我面前，递来一份三千两百万的暴雷借款保证书...             |
| v 📁 故事设定集 | 他以为脱节社会三年的全职太太是个任人宰割的软柿子，却不知我曾是顶级会计事务所...           |
|   [M] 设定.md   |                                                                                       |
|   [M] 小节大纲.md| ### 1.                                                                                |
|  ▶[M] 正文.md   | 深夜一点，大门被钥匙拧得咔嚓作响。                                                    |
| v 📁 示例代码库 | 顾诚带着一身刺鼻的浓烈酒气，脚步虚浮地跌撞进客厅，扑通一声跪倒在我面前...                 |
|   [TS] api.ts   | “知意，我对不起你和念念，公司彻底完了。”                                              |
|   [{}] config.json                                                                                      |
|   [T] run.log   |                                                                                       |
|-----------------|                                                                                       |
| 📁 示例小说项目  |                                                                                       |
| 共 5 个文件     |                                                                                       |
+---------------------------------------------------------------------------------------------------------+
```

### 2.1 区域划分与功能特征
1. **左侧侧边栏（导航与文件树）**：
   - **顶置工具栏**：
     - **目录树视图（📁）**：展示完整的层级目录、文件夹折叠与文件列表。
     - **列表视图（📄）**：按扁平列表展示文件/章节，适合纯顺序查阅。
     - **实时检索（🔍）**：模糊匹配文件名与文本内容。
     - **过滤与视图设置（⚙/Sliders）**：按文件类型筛选（全选 / 仅文档 / 仅代码）、排序规则等。
   - **多级文件树容器（File Tree）**：
     - 支持任意层级的文件夹递归嵌套展开/折叠。
     - **智能文件类型徽章/图标识别**：
       - `[M]` Markdown 类（`.md`, `.markdown`）
       - `[TS]` / `[JS]` / `[PY]` / `[{}]` / `[<>]` 代码与数据文件（`.ts`, `.tsx`, `.js`, `.py`, `.json`, `.html`, `.css` 等）
       - `[T]` / `[LOG]` 纯文本与日志类（`.txt`, `.log`, `.env`, `.ini`, `.csv` 等）
     - 高亮指示当前处于激活状态的文件（加粗、主题色高亮背景条）。
   - **底置状态栏（纯净信息展示）**：
     - 仅展示当前打开的项目名称、文件总数统计与快速导入入口，**无任何付费广告与升级卡片**。

2. **右侧主视窗（沉浸阅读与代码高亮视窗）**：
   - **顶置操作栏**：
     - **左侧**：侧边栏折叠/展开开关（一键收起左侧目录树，进入 100% 纯净全宽模式）。
     - **中间**：模式切换器（富文本预览 / 代码高亮 / 纯文本源码）、辅助开关（一键复制代码、显示/隐藏行号、换行折叠）。
     - **右侧**：浮动 TOC 目录大纲按钮（Markdown 模式下提取 `H1-H6` 标题，代码模式下支持函数/符号大纲）。
   - **自适应多模态渲染核心**：
     - **Markdown 引擎**：支持小说正文与文档排版、分级标题、段落防视觉疲劳。
     - **代码高亮引擎 (Code Engine)**：轻量语法着色、等宽字体、行号对齐、复制按钮。
     - **纯文本/日志引擎**：大文本防卡顿渲染、自动换行与行号显示。

---

## 3. 详细功能规格需求

### 3.1 支持格式矩阵 (Supported Formats Matrix)

| 类别 | 扩展名覆盖 | 默认查看器模式 | 专有特性 |
| :--- | :--- | :---: | :--- |
| **文档与小说** | `.md`, `.markdown` | Markdown 富文本排版 | 章节排版、TOC 目录大纲抽屉、段间距优化 |
| **前端/通用代码** | `.ts`, `.tsx`, `.js`, `.jsx`, `.json`, `.html`, `.css`, `.vue` | 代码高亮模式 | 语法高亮、等宽字体、一键复制代码、行号 |
| **后端/系统代码** | `.py`, `.java`, `.go`, `.rs`, `.c`, `.cpp`, `.sql`, `.sh`, `.bash` | 代码高亮模式 | 关键字高亮、注释淡化、长行换行开关 |
| **配置与序列化** | `.yaml`, `.yml`, `.toml`, `.env`, `.ini`, `.xml` | 代码高亮模式 | 键值高亮、层级缩进指示 |
| **纯文本与日志** | `.txt`, `.log`, `.csv` | 纯文本阅读模式 | 极速流畅加载、支持超长日志、自动折行 |

---

### 3.2 文件结构与多级目录树管理 (File Tree)

| 功能项 | 描述 | 优先级 |
| :--- | :--- | :---: |
| **多级树形渲染** | 支持文件夹递归嵌套，展开/折叠状态自动记忆 | P0 |
| **多类型文件识别** | 根据扩展名渲染专属图标（Markdown `M`、代码 `TS/PY/{}`、纯文本 `TXT`） | P0 |
| **内置演示数据包** | 首次安装内置示范项目（既包含小说设定/大纲/正文，也包含配置与代码示例） | P0 |
| **本地文件夹挂载** | 通过 File System Access API 一键选择并挂载本地文件夹 | P0 |
| **ZIP 压缩包导入** | 支持直接拖拽或读取本地 ZIP 压缩包解析生成目录树 | P1 |
| **多文件/拖拽导入** | 支持多选文件直接拖入窗口阅读 | P1 |
| **树形实时检索** | 顶部搜索框实时过滤文件名，自动高亮并展开匹配节点 | P0 |
| **文件类型过滤** | 顶栏过滤按钮可一键筛选：全部 / 仅文档 / 仅代码 / 仅文本 | P1 |

---

### 3.3 多模态阅读与查看引擎 (Reader & Viewer Engines)

#### 3.3.1 Markdown 阅读引擎
- 标准 Markdown 语法安全解析（标题、加粗、引用、列表、表格、代码块）。
- 专为小说、长文优化的阅读排版（字号、行距、可选首行缩进）。
- 自动提取正文内 `H1-H6` 标题生成浮动大纲（TOC）抽屉，支持点击平滑锚点跳转。

#### 3.3.2 代码阅读引擎 (Code Viewer)
- **语法着色**：基于轻量高亮库（Prism.js 等），支持常见 20+ 种主流语言。
- **行号渲染**：左侧对齐行号栏，支持一键切换开启/关闭。
- **一键复制代码**：顶部或右上角悬浮“复制全文 / 复制代码块”按钮，附带成功 Toast。
- **等宽字体与换行**：选用专业编程字体栈（Fira Code, JetBrains Mono, Consolas），支持自由切换“自动换行（Word Wrap）”与“横向滚动”。

#### 3.3.3 纯文本与日志模式 (Plain Text / Log)
- 高性能渲染大篇幅日志或长文本。
- 等宽或正文字体自由切换。
- 支持全文关键词高亮与检索定位。

#### 3.3.4 通用阅读体验与工具
- **阅读记忆**：自动记录每个文件的最后滚动位置，二次打开无缝接续。
- **侧边栏一键折叠**：顶置左上角切换按钮，平滑隐藏左侧文件树，主内容区全宽展开。
- **阅读主题切换**：经典纯白、护眼豆沙绿、复古羊皮纸、沉浸暗黑（Dark Mode）。

---

### 3.4 数据存储与安全策略 (Storage Architecture)

1. **混合存储策略**：
   - **配置与状态（`chrome.storage.local`）**：
     - 当前活动文件路径、侧栏折叠状态、主题模式、字号、换行开关等。
     - 文件夹展开/折叠状态集合。
   - **文件内容与树结构（`IndexedDB`）**：
     - 本地大容量持久化，不受 10MB 配额限制，毫秒级读取超大代码文件与长篇小说。
2. **离线与数据隐私安全**：
   - 100% 纯本地运行与离线解析，无任何网络请求上传本地文件，完全保护隐私与代码资产。

---

## 4. 技术架构规划 (Technical Stack)

- **框架体系**：WXT + React 19 + TypeScript。
- **应用路径**：`apps/tree-reader`。
- **共享工具复用**：
  - `@workspace/shared-utils`：Chrome API 工具与打包修复
  - **无需依赖商业化授权库**（保持完全纯净、轻量与开源体验）
- **代码高亮库**：Prism.js（轻量级，性能极佳，按需着色）。
- **图标体系**：Lucide-react 图标套件。

---

## 5. 待确认细节

1. **大文件防卡顿策略**：
   - 对于超过 500KB 的大文本或日志文件，是否默认采用纯文本分页/截断加载以确保侧边栏极致流畅？
2. **代码编辑需求**：
   - 是否在 V1 版本中保持“纯阅读/查看”为主，还是直接支持简单修改保存？
